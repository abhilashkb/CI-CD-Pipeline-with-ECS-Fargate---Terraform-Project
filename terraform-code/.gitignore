# Local .terraform directories



.terraform/
*.tfstate
*.tfstate.backup
# Crash log files
crash.log
crash.*.log

# Exclude override files (unless you really want to commit them)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include tfvars files with sensitive data
*.tfvars
*.tfvars.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Lock file (optional — keep if you want reproducible provider versions)
.terraform.lock.hcl
